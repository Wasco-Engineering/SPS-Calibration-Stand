# DEPRECATED — do not edit this file for production stands.
# Use configs/<hostname>/quality_cal_config.yaml (hostname auto-selected).

app:
  name: Quality Calibration
  version: 0.1.0
  notice: Use configs/<hostname>/quality_cal_config.yaml — this root file is a fallback only.

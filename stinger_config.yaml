# DEPRECATED — do not edit this file for production stands.
#
# Per-computer configs live in the repo under:
#   configs/<hostname>/stinger_config.yaml
#   configs/<hostname>/quality_cal_config.yaml
#
# Runtime selection is automatic from the machine hostname
# (see app.core.paths and configs/README.md).
#
# This root file remains only as a last-resort fallback for older checkouts.

app:
  name: Stinger
  version: 1.0.0
  notice: Use configs/<hostname>/stinger_config.yaml — this root file is a fallback only.
test_parameters:
  equipment_id: UNSET-USE-HOST-CONFIG
